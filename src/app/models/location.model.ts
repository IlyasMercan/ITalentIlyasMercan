export class Location {
  companyName: string;
  eventAddress: string;

  constructor(companyName: string, eventAddress: string) {
    this.companyName = companyName;
    this.eventAddress = eventAddress;
  }
}
